<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.snahva.sensorsapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_list_sensor"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SensorCahayaActivity"></activity>
        <activity android:name=".DashboardMenuActivity" />
        <activity android:name=".BallGameActivity" />
        <activity
            android:name=".OrientationSensorActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".LightSensorActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ListSensorsActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>